services:
  spr-opencanary:
    extends:
      file: docker-compose.yml
      service: spr-opencanary
    image: ghcr.io/spr-networks/spr-opencanary:latest
    runtime: ${SPR_KRUN_RUNTIME:-spr-krun}
    annotations:
      krun.cpus: "${SPR_KRUN_CPUS:-1}"
      krun.ram_mib: "${SPR_KRUN_RAM_MIB:-128}"
      krun.tap_name: "kruntap0"
      krun.net_uplink: "eth0"
      krun.vsock_path: "/state/plugins/spr-opencanary-krun/socket.sock"
      krun.vsock_port: "4040"
    environment:
      SPR_KRUN_PLUGIN_SOCKET: "/run/spr-krun-plugin/spr-opencanary.sock"
      SPR_KRUN_VSOCK_PORT: "4040"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    devices:
      - "/dev/net/tun:/dev/net/tun"

networks:
  canarynet:
    name: spr-opencanary
    driver_opts:
      com.docker.network.bridge.name: spr-opencanary
    ipam:
      config:
        - subnet: 172.30.119.0/24
